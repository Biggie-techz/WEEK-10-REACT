<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Log in</title>

    <style>
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: "Roboto", sans-serif;
      }

      body {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        background-image: url(../images/login-bg.jpg);
        background-size: cover;
        background-position: center;
        backdrop-filter: blur(5px);
      }

      :root {
        --primary-color: royalblue;
        --secondary-color: rgba(105, 105, 105, 0.397);
        --secondary-color2: grey;
      }

      a {
        cursor: pointer;
      }
      .form {
        display: flex;
        flex-direction: column;
        gap: 10px;
        width: 450px;
        background-color: #ffffffc7;
        backdrop-filter: blur(20px);
        padding: 40px 20px;
        padding-inline-end: 40px;
        border-radius: 20px;
        position: relative;
      }

      ::-webkit-outer-spin-button,
      ::-webkit-inner-spin-button {
        display: none;
      }

      .title {
        font-size: 3rem;
        color: var(--primary-color);
        font-weight: 600;
        letter-spacing: -1px;
        position: relative;
        display: flex;
        align-items: center;
        padding-left: 30px;
      }

      .title::before,
      .title::after {
        position: absolute;
        content: "";
        height: 16px;
        width: 16px;
        border-radius: 50%;
        left: 0px;
        background-color: var(--primary-color);
      }

      .title::before {
        width: 18px;
        height: 18px;
        background-color: var(--primary-color);
      }

      .title::after {
        width: 18px;
        height: 18px;
        animation: pulse 1s linear infinite;
      }

      .message,
      .signup {
        color: rgba(88, 87, 87, 0.822);
        font-size: 1rem;
      }

      .signup {
        text-align: center;
      }

      .signup a {
        color: var(--primary-color);
      }

      .signup a:hover {
        text-decoration: underline var(--primary-color);
      }

      .flex {
        display: flex;
        width: 100%;
        gap: 6px;
      }

      .form label {
        position: relative;
      }

      .form label .input {
        width: 100%;
        padding: 10px 12px 20px 10px;
        outline: 0;
        border: 1px solid var(--secondary-color);
        background-color: transparent;
        border-radius: 10px;
        font-size: 1rem;
      }

      .form label .input + span {
        position: absolute;
        left: 10px;
        top: 15px;
        color: var(--secondary-color2);
        font-size: 1.1rem;
        cursor: text;
        transition: 0.3s ease;
      }

      .form label .input:focus,
      .form label .input:valid {
        outline: 2px solid var(--secondary-color);
      }

      .form label .input:focus + span,
      .form label .input:valid + span {
        top: 30px;
        font-size: 0.8rem;
        font-weight: 500;
        padding: 2px 5px;
        border-radius: 5px;
        border-right: 1px solid var(--secondary-color);
        border-left: 1px solid var(--secondary-color);
      }

      .submit,
      .backToSignup {
        border: none;
        outline: none;
        background-color: var(--primary-color);
        padding: 10px;
        border-radius: 10px;
        text-align: center;
        text-decoration: none;
        color: #fff;
        font-size: 1.5rem;
        transform: 0.3s ease;
        width: 100%;
        cursor: pointer;
      }

      .submit:hover {
        background-color: rgb(56, 90, 194);
      }
    </style>
  </head>
  <body>
    <form class="form" id="form">
      <p class="title">Log in</p>
      <p class="message">Sign in now to get full access to our app.</p>
      <label>
        <input required type="email" class="input" id="email" />
        <span id="emailPlaceholder">Email</span>
      </label>
      <label>
        <input required type="password" class="input" id="password" />
        <span id="passwordPlaceholder">Password</span>
      </label>
      <button class="btn submit" id="submit" type="button">Log in</button>
      <p class="signup">
        Don't have an account? <a href="signup" id="dontHaveAnAccount">Sign up</a>
      </p>
    </form>
  </body>
</html>
